<div align="center">
  <a href="https://vuicons.qiboz.top/" target="_blank">
    <img src="./assets/logo.png" width="120" alt="VU-Icons Logo" />
  </a>
</div>

# VU-Icons 组件库使用指南

> 🌐 **官方网站**：[https://vuicons.qiboz.top/](https://vuicons.qiboz.top/)

VU-Icons 是一个专为 Vue 3 和 UniApp 设计的高质量 SVG 图标组件库。它轻量、灵活且易于使用，支持 Tree Shaking（按需引入），并内置了完整的 TypeScript 类型声明。

## 📦 安装

首先，你需要将组件库安装到你的项目中。

### 使用 NPM

```bash
npm install vu-icons
```

### 使用 Yarn

```bash
yarn add vu-icons
```

---

## 🚀 快速开始

VU-Icons 支持 Vue 3 和 UniApp 双框架。请根据你的项目类型选择相应的引入方式。

### 1. Vue 3 项目中使用

在 Vue 3 项目中，推荐使用 `VuIcon` 核心组件 + `name` 属性的方式引入图标。

```vue
<script setup lang="ts">
// 推荐方式：导入核心组件
import VuIcon from 'vu-icons/icon'
</script>

<template>
  <div class="icon-container">
    <!-- 基础用法 -->
    <VuIcon name="home" />
    
    <!-- 自定义颜色和尺寸 -->
    <VuIcon name="search" color="#1890ff" :size="32" />
    
    <!-- 添加旋转动画 -->
    <VuIcon name="settings" spin color="#52c41a" />
  </div>
</template>

<style scoped>
.icon-container {
  display: flex;
  gap: 16px;
  align-items: center;
}
</style>
```

也支持按需引入包装组件：

```vue
<script setup lang="ts">
// 按需引入包装组件
import { VuHome, VuSearch, VuSettings } from 'vu-icons'
</script>

<template>
  <VuHome :size="24" color="#333" />
  <VuSearch :size="32" color="#1890ff" />
  <VuSettings spin color="#52c41a" />
</template>
```

### 2. UniApp 项目中使用

在 UniApp 项目中，**必须直接导入 `VuIcon` 核心组件**（不支持从 `vu-icons/uniapp` 间接导入包装组件）。

```vue
<script>
// ✅ 正确：直接导入 .vue 文件
import VuIcon from 'vu-icons/uniapp/icon'

export default {
  components: { VuIcon }
}
</script>

<template>
  <view class="content">
    <view class="icon-row">
      <text>基础图标：</text>
      <VuIcon name="home" />
    </view>
    
    <view class="icon-row">
      <text>自定义样式：</text>
      <VuIcon name="search" color="#ff4d4f" :size="32" />
    </view>
    
    <view class="icon-row">
      <text>加载中：</text>
      <VuIcon name="loading" spin color="#1890ff" />
    </view>
  </view>
</template>
```

> ⚠️ **UniApp 重要提示**：由于微信小程序编译限制，**不能**使用 `import { VuHome } from 'vu-icons/uniapp'` 的方式导入包装组件，必须使用 `VuIcon` 核心组件 + `name` 属性。

---

## 🎨 组件属性 (Props)

### VuIcon 核心组件

| 属性名 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| **name** | `string` | `''` | 图标名称（推荐使用）。如 `'home'`、`'search'`、`'star'` 等 |
| **icon** | `string` | `''` | 图标名称（别名，与 `name` 相同，优先使用 `name`） |
| **size** | `number` \| `string` | `24` | 图标的尺寸。<br>• 传入数字时单位为 `px`（如 `24`）。<br>• 传入字符串时支持任意 CSS 单位（如 `'2rem'`, `'100%'`）。 |
| **color** | `string` | `'currentColor'` | 图标的颜色。<br>• 默认为 `'currentColor'`，在 Vue3 中继承父元素字体颜色，在 UniApp 中默认为 `#333333`。<br>• 支持十六进制、RGB、颜色名称等所有 CSS 颜色值。 |
| **spin** | `boolean` | `false` | 是否开启旋转动画。<br>• 适用于 `loading`, `refresh` 等需要动效的场景。<br>• 实际上**任何图标**都可以通过此属性开启旋转。 |

### 包装组件（VuHome、VuSearch 等）

包装组件仅支持 Vue3 项目，Props 与 VuIcon 相同，但不需要 `name`/`icon` 属性（图标名称已内置）。

---

## 💡 进阶用法

### 1. 动态控制

你可以通过绑定变量来动态改变图标的状态。

```vue
<script setup>
import { ref } from 'vue'
import VuIcon from 'vu-icons/icon'

const isLoading = ref(false)

const handleRefresh = () => {
  isLoading.value = true
  setTimeout(() => {
    isLoading.value = false
  }, 2000)
}
</script>

<template>
  <button @click="handleRefresh">
    <!-- 点击时图标旋转 -->
    <VuIcon name="refresh" :spin="isLoading" />
    刷新数据
  </button>
</template>
```

### 2. 动态切换图标

通过绑定 `name` 属性来动态切换不同的图标。

```vue
<script setup>
import { ref } from 'vue'
import VuIcon from 'vu-icons/icon'

const isExpanded = ref(false)
</script>

<template>
  <VuIcon :name="isExpanded ? 'chevron-up' : 'chevron-down'" @click="isExpanded = !isExpanded" />
</template>
```

### 3. 在 TypeScript 中使用

VU-Icons 内置了完整的类型定义文件 (`.d.ts`)，因此在 TypeScript 项目中你无需进行任何额外配置，即可获得完善的代码提示和类型检查。

---

## 🔧 技术原理

VU-Icons 使用 **CSS Mask + URL编码 SVG** 方案渲染图标，在 Vue3 和 UniApp 中统一实现：

```
SVG content → encodeURIComponent 编码 → data:image/svg+xml,... → mask-image (遮罩)
                                                              ↓
                                                  background-color (着色)
```

- **mask-image**：SVG 作为遮罩形状，定义图标的轮廓
- **background-color**：通过 CSS 变量动态传递，控制图标颜色
- **CSS 变量**：解决微信小程序内联 style 不支持 `mask-image` 属性的限制

此方案的优势：
- ✅ 不使用字体文件，无需额外配置
- ✅ 不使用 base64，微信小程序原生支持 URL编码 SVG
- ✅ 不使用条件编译，所有平台统一实现
- ✅ 矢量渲染，任何尺寸都清晰

---

## ❓ 常见问题 (FAQ)

**Q: 这个库支持 Tree Shaking 吗？**
A: 支持。只要你的打包工具（如 Vite, Webpack）支持 Tree Shaking，未使用的图标代码会在构建时自动被移除，不会增加最终包的体积。

**Q: 为什么在 UniApp 中不能用 `import { VuHome } from 'vu-icons/uniapp'`？**
A: uniapp 编译器对通过 JS 中转导出的 Vue 组件不会生成完整的小程序组件四件套（.wxml/.js/.json/.wxss），导致在微信小程序中不显示。必须直接导入 `.vue` 文件：`import VuIcon from 'vu-icons/uniapp/icon'`。

**Q: 在 UniApp 中需要配置字体文件吗？**
A: 不需要。VU-Icons v1.5.0+ 使用 CSS Mask + SVG 方案，不再依赖字体文件，无需任何额外配置。

**Q: Vue3 和 UniApp 的 API 一致吗？**
A: 是的，`VuIcon` 核心组件在两个平台使用完全相同的 Props（`name`/`icon`/`size`/`color`/`spin`），只是底层 HTML 元素不同（`<div>` vs `<view>`）。

**Q: 如何修改默认的描边粗细？**
A: 目前组件默认使用了 `stroke-width="2"`。如果需要修改，可以在 SVG 源文件中调整，然后重新构建。

---

## 📝 贡献与反馈

如果你在使用过程中遇到问题，或者希望添加新的图标，欢迎在 GitHub 仓库提交 Issue 或 Pull Request。

祝开发愉快！🎉
