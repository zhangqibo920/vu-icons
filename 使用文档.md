<div align="center">
  <img src="./assets/logo.png" width="120" alt="VU-Icons Logo" />
</div>

# VU-Icons 组件库使用指南

VU-Icons 是一个专为 Vue 3 和 UniApp 设计的高质量 SVG 图标组件库。它轻量、灵活且易于使用，支持 Tree Shaking（按需引入），并内置了完整的 TypeScript 类型声明。

## 📦 安装

首先，你需要将组件库安装到你的项目中。

### 使用 NPM

```bash
npm install vu-icons
```

### 使用 Yarn

```bash
yarn add vu-icons
```

---

## 🚀 快速开始

VU-Icons 支持 Vue 3 和 UniApp 双框架。请根据你的项目类型选择相应的引入方式。

### 1. Vue 3 项目中使用

在 Vue 3 项目中，你可以直接从 `vu-icons` 包中引入所需的图标组件。

```vue
<script setup lang="ts">
// 按需引入图标组件
import { VuHome, VuSearch, VuSettings } from 'vu-icons'
</script>

<template>
  <div class="icon-container">
    <!-- 基础用法 -->
    <VuHome />
    
    <!-- 自定义颜色和尺寸 -->
    <VuSearch color="#1890ff" :size="32" />
    
    <!-- 添加旋转动画 -->
    <VuSettings spin color="#52c41a" />
  </div>
</template>

<style scoped>
.icon-container {
  display: flex;
  gap: 16px;
  align-items: center;
}
</style>
```

### 2. UniApp 项目中使用

在 UniApp 项目中，请从 `vu-icons/uniapp` 路径引入组件。这确保了组件在小程序、App 等平台的兼容性。

```vue
<script setup lang="ts">
// 注意路径：从 'vu-icons/uniapp' 引入
import { VuHome, VuSearch, VuLoading } from 'vu-icons/uniapp'
</script>

<template>
  <view class="content">
    <view class="icon-row">
      <text>基础图标：</text>
      <VuHome />
    </view>
    
    <view class="icon-row">
      <text>自定义样式：</text>
      <VuSearch color="#ff4d4f" size="40px" />
    </view>
    
    <view class="icon-row">
      <text>加载中：</text>
      <VuLoading spin color="#1890ff" />
    </view>
  </view>
</template>
```

---

## 🎨 组件属性 (Props)

所有图标组件都支持以下属性，你可以通过这些属性轻松定制图标的外观和行为。

| 属性名 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| **size** | `number` \| `string` | `24` | 图标的尺寸。<br>• 传入数字时单位为 `px`（如 `24`）。<br>• 传入字符串时支持任意 CSS 单位（如 `'2rem'`, `'100%'`）。 |
| **color** | `string` | `'currentColor'` | 图标的颜色。<br>• 默认为 `'currentColor'`，即继承父元素的字体颜色。<br>• 支持十六进制、RGB、颜色名称等所有 CSS 颜色值。 |
| **className** | `string` | `''` | 自定义类名，方便通过 CSS 进行更复杂的样式控制。 |
| **spin** | `boolean` | `false` | 是否开启旋转动画。<br>• 适用于 `VuLoading`, `VuRefresh` 等需要动效的场景。<br>• 实际上**任何图标**都可以通过此属性开启旋转。 |

---

## 💡 进阶用法

### 1. 动态控制

你可以通过绑定变量来动态改变图标的状态。

```vue
<script setup>
import { ref } from 'vue'
import { VuRefresh } from 'vu-icons'

const isLoading = ref(false)

const handleRefresh = () => {
  isLoading.value = true
  setTimeout(() => {
    isLoading.value = false
  }, 2000)
}
</script>

<template>
  <button @click="handleRefresh">
    <!-- 点击时图标旋转 -->
    <VuRefresh :spin="isLoading" />
    刷新数据
  </button>
</template>
```

### 2. 结合 CSS 类名

使用 `className` 属性可以方便地添加 hover 效果或其他样式。

```vue
<template>
  <VuHome className="my-hover-icon" :size="32" />
</template>

<style>
.my-hover-icon {
  transition: transform 0.3s;
  cursor: pointer;
}

.my-hover-icon:hover {
  color: #1890ff;
  transform: scale(1.2); /* 鼠标悬停时放大 */
}
</style>
```

### 3. 在 TypeScript 中使用

VU-Icons 内置了完整的类型定义文件 (`.d.ts`)，因此在 TypeScript 项目中你无需进行任何额外配置，即可获得完善的代码提示和类型检查。

---

## ❓ 常见问题 (FAQ)

**Q: 这个库支持 Tree Shaking 吗？**
A: 支持。只要你的打包工具（如 Vite, Webpack）支持 Tree Shaking，未使用的图标代码会在构建时自动被移除，不会增加最终包的体积。

**Q: 为什么在 UniApp 中要从 `vu-icons/uniapp` 引入？**
A: UniApp 的非 H5 平台（如微信小程序）对 SVG 的支持有限制。`vu-icons/uniapp` 下的组件针对这些平台做了特殊处理（在非 H5 平台使用 `rich-text` 节点渲染 SVG），以确保跨端兼容性。

**Q: 如何修改默认的描边粗细？**
A: 目前组件默认使用了 `stroke-width="2"`。如果需要全局修改，可以通过 CSS 选择器覆盖 SVG 的样式，或者在特定场景下通过 `className` 定制。

---

## 📝 贡献与反馈

如果你在使用过程中遇到问题，或者希望添加新的图标，欢迎在 GitHub 仓库提交 Issue 或 Pull Request。

祝开发愉快！🎉
